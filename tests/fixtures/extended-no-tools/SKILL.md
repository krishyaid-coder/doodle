---
name: extended-no-tools
description: Extended dialect skill that uses Bash but forgets allowed-tools. Use when running shell commands to deploy.
version: 1.0.0
author: someone
tags: [deploy, shell]
---

# Extended No Tools

Run the following via Bash:

```bash
./deploy.sh
```

But also: Bash and Write are referenced in this prose, outside fences, on this very line.
